<Window x:Class="AccountStorage.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
        xmlns:ic="http://schemas.fontawesome.com/icons/"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:AccountStorage"
        xmlns:vm="clr-namespace:AccountStorage.ViewModels"
        xmlns:b="clr-namespace:AccountStorage.Behaviors"
        Height="450" Width="800" Title="Keys Keeper" WindowStyle="None"
        Background="White"
        WindowState="Maximized">
    <Window.Resources>
        <Style TargetType="ic:FontAwesome">
            <Setter Property="FontSize" Value="30"/>
            <Setter Property="Margin" Value="10 5"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="Red"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Button" x:Key="EmptyButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" RecognizesAccessKey="True" 
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Window.DataContext>
        <vm:MainWindowVM/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Border BorderBrush="Black" BorderThickness="0 0 0 3">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Command="{Binding MinimizeWindowCommand}" CommandParameter="{Binding }" Style="{StaticResource EmptyButton}">
                    <ic:FontAwesome Icon="Solid_WindowMinimize" FontSize="30"/>
                    <i:Interaction.Behaviors>
                        <b:MinimizeWindow/>
                    </i:Interaction.Behaviors>
                </Button>
                <Button Style="{StaticResource EmptyButton}">
                    <ic:FontAwesome Icon="Solid_WindowRestore"/>
                    <i:Interaction.Behaviors>
                        <b:ChangeWindowState/>
                    </i:Interaction.Behaviors>
                </Button>
                <Button Style="{StaticResource EmptyButton}">
                    <ic:FontAwesome Icon="Solid_WindowClose" FontSize="30"/>
                    <i:Interaction.Behaviors>
                        <b:CloseWindow/>
                    </i:Interaction.Behaviors>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
